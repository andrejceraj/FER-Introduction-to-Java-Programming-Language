package hr.fer.zemris.java.hw06.shell;

/**
 * {@link MyShell} status. Status should be {@code CONTINUE} if the shell should
 * continue to run normally; status should be {@code TERMINATE} if the shell
 * should stop running.
 * 
 * @author Andrej Ceraj
 *
 */
public enum ShellStatus {
	CONTINUE, TERMINATE
}
